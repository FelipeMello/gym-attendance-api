server:
  port: ${PORT:8000}
  servlet:
    context-path: /api
spring:
  datasource:
    url: ${URL:jdbc:postgresql://my_postgres:5432/mygym}
    username: ${USERNAME:gymapi}
    password: ${PASSWORD:gym1243OSOKpadpas-69}
  jpa:
    hibernate:
      ddl-auto:
        - update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
logging:
  level:
    org.hibernate.sql: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE